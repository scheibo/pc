'use strict'; /* eslint-env mocha */
var assert = require('assert');
var pc = require('../');
var Distance = pc.Distance;

suite('pc.Distance', () => {
  test('toString()', () => {
  });
  test('fromString()', () => {
  });
});
